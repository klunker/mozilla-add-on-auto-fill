{
  "name": "autofill-firefox-extension",
  "version": "1.0.0",
  "description": "Minimalist Firefox extension for form autofilling with alias support.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && esbuild src/background/background.ts --bundle --outfile=dist/background.js --platform=browser && esbuild src/content/content.ts --bundle --outfile=dist/content.js --platform=browser",
    "preview": "vite preview",
    "test": "vitest",
    "web-ext:run": "web-ext run -s dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "@types/jsdom": "^28.0.1",
    "@types/webextension-polyfill": "^0.10.0",
    "jsdom": "^29.0.2",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "web-ext": "^8.0.0",
    "webextension-polyfill": "^0.10.0"
  }
}
